<!DOCTYPE html>
<html>
  <head>
    <title><%= blog.title %></title>
    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
  </head>
  <body class="bg-gray-100 h-screen flex items-center justify-center">
    <div class="max-w-3xl p-4 bg-white rounded-lg shadow-lg">
      <h1 class="text-3xl font-bold text-gray-800 mb-4"><%= blog.title %></h1>
      <p class="text-lg text-gray-700 leading-relaxed"><%= blog.content %></p>
    </div >
    <div class="flex flex-col" >
    <a href="./<%= blog.id %>/update"><button class="px-4 py-2 bg-blue-800 text-white">Update this Post</button></a>
    <a href="./<%= blog.id %>/delete"><button class="px-4 py-2 bg-red-800 text-white">Delete this Post</button></a>
    </div>
  </body>
</html>
